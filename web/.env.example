# Optional — leave blank to use Vite proxy → http://127.0.0.1:8081
VITE_API_BASE_URL=

# Google Places (venue photos only). Never commit a real key.
VITE_GOOGLE_PLACES_API_KEY=

# Supabase (Phase 2 auth). Placeholders only in git.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
